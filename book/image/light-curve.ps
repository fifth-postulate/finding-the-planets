%!PS-Adobe-1.0

/square {
    [/s /y /x] dup length 0 add dict begin {exch def} forall
    x s 2 div sub y s 2 div sub moveto
    s 0 rlineto
    0 s rlineto
    s neg 0 rlineto
    closepath
    end
} def

/transit {
    [/d /S /s /y /x] dup length 0 add dict begin {exch def} forall
    x y S square
    x d add y s square
    end
} def

save /clean-slate exch def
75 25.4 div dup scale

/x0 210 2 div def
/y0 297 2 div def
/s 5 def
/S 25 def


x0 y0 S s add 3 mul add s S S neg transit
x0 y0 S s add 2 mul add s S S s add 2 div neg transit
x0 y0 S s add 1 mul add s S S s sub 2 div neg transit
x0 y0 S s add 0 mul add s S 0 transit
x0 y0 S s add -1 mul add s S S s sub 2 div transit
x0 y0 S s add -2 mul add s S S s add 2 div transit
x0 y0 S s add -3 mul add s S S transit

stroke
clean-slate restore
